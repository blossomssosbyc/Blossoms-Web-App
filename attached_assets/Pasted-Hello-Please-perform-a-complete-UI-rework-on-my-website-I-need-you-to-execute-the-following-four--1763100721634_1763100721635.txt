Hello\! Please perform a complete UI rework on my website. I need you to execute the following four tasks:

## 1\. Update the Global Color Scheme

Please find the main CSS file (`index.css` or `global.css`) and/or the `tailwind.config.js` file. Replace the existing color palette across the entire site with the following:

  * **Primary:** `#001F3F` (Use this for primary backgrounds, headers, and main text)
  * **Secondary:** `#3A6D8C` (Use this for interactive elements like buttons, links, and hover states)
  * **Tertiary:** `#6A9AB0` (Use this for borders, dividers, and secondary text)
  * **Quaternary:** `#EAD8B1` (Use this for special "call to action" buttons or accent sections)

Ensure these new colors are applied consistently everywhere.

-----

## 2\. Rework the Navbar and Create a New Menu Page

This is a multi-step task.

**A. Install Dependency:**
First, please run this command in the shell:

```bash
npm install gsap
```

**B. Modify the Existing Navbar Component:**
Locate the current navbar component. Remove all the existing navigation links and buttons inside it. Replace them with **one single button or link** that says "Menu". This button should navigate the user to a new page route: `/menu`.

**C. Create the New `FlowingMenu.jsx` Component:**
Create a new component file at `src/components/FlowingMenu.jsx` and add the following code:

```jsx
import React from 'react';
import { gsap } from 'gsap';

interface MenuItemProps {
  link: string;
  text: string;
  image: string;
}

interface FlowingMenuProps {
  items?: MenuItemProps[];
}

const FlowingMenu: React.FC<FlowingMenuProps> = ({ items = [] }) => {
  return (
    <div className="w-full h-full overflow-hidden">
      <nav className="flex flex-col h-full m-0 p-0">
        {items.map((item, idx) => (
          <MenuItem key={idx} {...item} />
        ))}
      </nav>
    </div>
  );
};

const MenuItem: React.FC<MenuItemProps> = ({ link, text, image }) => {
  const itemRef = React.useRef<HTMLDivElement>(null);
  const marqueeRef = React.useRef<HTMLDivElement>(null);
  const marqueeInnerRef = React.useRef<HTMLDivElement>(null);

  const animationDefaults = { duration: 0.6, ease: 'expo' };

  const findClosestEdge = (mouseX: number, mouseY: number, width: number, height: number): 'top' | 'bottom' => {
    const topEdgeDist = Math.pow(mouseX - width / 2, 2) + Math.pow(mouseY, 2);
    const bottomEdgeDist = Math.pow(mouseX - width / 2, 2) + Math.pow(mouseY - height, 2);
    return topEdgeDist < bottomEdgeDist ? 'top' : 'bottom';
  };

  const handleMouseEnter = (ev: React.MouseEvent<HTMLAnchorElement>) => {
    if (!itemRef.current || !marqueeRef.current || !marqueeInnerRef.current) return;
    const rect = itemRef.current.getBoundingClientRect();
    const edge = findClosestEdge(ev.clientX - rect.left, ev.clientY - rect.top, rect.width, rect.height);

    const tl = gsap.timeline({ defaults: animationDefaults });
    tl.set(marqueeRef.current, { y: edge === 'top' ? '-101%' : '101%' })
      .set(marqueeInnerRef.current, { y: edge === 'top' ? '101%' : '-101%' })
      .to([marqueeRef.current, marqueeInnerRef.current], { y: '0%' });
  };

  const handleMouseLeave = (ev: React.MouseEvent<HTMLAnchorElement>) => {
    if (!itemRef.current || !marqueeRef.current || !marqueeInnerRef.current) return;
    const rect = itemRef.current.getBoundingClientRect();
    const edge = findClosestEdge(ev.clientX - rect.left, ev.clientY - rect.top, rect.width, rect.height);

    const tl = gsap.timeline({ defaults: animationDefaults }) as TimelineMax;
    tl.to(marqueeRef.current, { y: edge === 'top' ? '-101%' : '101%' }).to(marqueeInnerRef.current, {
      y: edge === 'top' ? '101%' : '-101%'
    });
  };

  const repeatedMarqueeContent = React.useMemo(() => {
    return Array.from({ length: 4 }).map((_, idx) => (
      <React.Fragment key={idx}>
        <span className="text-[#060010] uppercase font-normal text-[4vh] leading-[1.2] p-[1vh_1vw_0]">{text}</span>
        <div
          className="w-[200px] h-[7vh] my-[2em] mx-[2vw] p-[1em_0] rounded-[50px] bg-cover bg-center"
          style={{ backgroundImage: `url(${image})` }}
        />
      </React.Fragment>
    ));
  }, [text, image]);

  return (
    <div className="flex-1 relative overflow-hidden text-center shadow-[0_-1px_0_0_#fff]" ref={itemRef}>
      <a
        className="flex items-center justify-center h-full relative cursor-pointer uppercase no-underline font-semibold text-white text-[4vh] hover:text-[#060010] focus:text-white focus-visible:text-[#060010]"
        href={link}
        onMouseEnter={handleMouseEnter}
        onMouseLeave={handleMouseLeave}
      >
        {text}
      </a>
      <div
        className="absolute top-0 left-0 w-full h-full overflow-hidden pointer-events-none bg-white translate-y-[101%]"
        ref={marqueeRef}
      >
        <div className="h-full w-[200%] flex" ref={marqueeInnerRef}>
          <div className="flex items-center relative h-full w-[200%] will-change-transform animate-marquee">
            {repeatedMarqueeContent}
          </div>
        </div>
      </div>
    </div>
  );
};

export default FlowingMenu;
```

**D. Create the New Menu Page:**
Create a new page component (e.g., `src/pages/MenuPage.jsx`) that will live at the `/menu` route. Import and use the `FlowingMenu` component here.

```jsx
import React from 'react';
import FlowingMenu from '../components/FlowingMenu'; // Adjust path if needed

// Demo items. You can replace these with your actual page links.
const demoItems = [
  { link: '/', text: 'Home', image: 'https://picsum.photos/600/400?random=1' },
  { link: '/about', text: 'About', image: 'https://picsum.photos/600/400?random=2' },
  { link: '/services', text: 'Services', image: 'https://picsum.photos/600/400?random=3' },
  { link: '/contact', text: 'Contact', image: 'https://picsum.photos/600/400?random=4' }
];

const MenuPage = () => {
  return (
    // This creates a full-screen menu.
    <div style={{ height: '100vh', width: '100vw', position: 'relative', backgroundColor: '#001F3F' }}> 
      <FlowingMenu items={demoItems} />
    </div>
  );
};

export default MenuPage;
```

*(Note: Please also update the `App.jsx` or main router file to add the new route `/menu` that renders this `MenuPage` component.)*

**E. Update `tailwind.config.js`:**
Finally, update `tailwind.config.js` to include the necessary animations for the menu:

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      translate: {
        '101': '101%',
      },
      keyframes: {
        marquee: {
          'from': { transform: 'translateX(0%)' },
          'to': { transform: 'translateX(-50%)' }
        }
      },
      animation: {
        marquee: 'marquee 15s linear infinite'
      }
      // Make sure to add the new color palette here as well!
      // colors: {
      //   primary: '#001F3F',
      //   secondary: '#3A6D8C',
      //   tertiary: '#6A9AB0',
      //   quaternary: '#EAD8B1',
      // }
    }
  },
  plugins: [],
};
```

-----

## 3\. Update the Font

Please change the default font for the entire website to a modern, intuitive, and highly readable sans-serif font.

1.  Import the **"Inter"** font from Google Fonts into the `index.html` file.
2.  Set `'Inter', sans-serif` as the default `font-family` in the `body` tag in your global CSS file and also in the `theme` section of `tailwind.config.js`.

-----

## 4\. Add Fade-In Animations

To make the site render more stylishly, please add a subtle fade-in animation to the main content sections of each page.

1.  In your global CSS file (`index.css` or `global.css`), add these keyframes:

    ```css
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(15px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .fade-in-on-load {
      animation: fadeIn 0.7s ease-out forwards;
    }
    ```

2.  Apply the `fade-in-on-load` class to the main content wrapper `div` or `<main>` tag on each page component (like `HomePage.jsx`, `AboutPage.jsx`, etc.) so they fade in gently when the user navigates to them.

Thank you\!